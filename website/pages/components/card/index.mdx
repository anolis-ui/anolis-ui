import { x } from "@xstyled/emotion";
import { Card, Button, Typography} from "anolis-ui";
import { CodeBlock } from "components/CodeBlock";
import { ComponentDisplay } from "components/ComponentDisplay";
import { ComponentSandbox } from "components/ComponentSandbox";
import Head from "components/Head";

<Head title="Card" />

# Card

```tsx
import { Card } from "anolis-ui";

interface CardProps extends SystemProps {
  _header?: PseudoProp;
  header?: Renderable;

  _title?: PseudoProp;
  title?: Renderable;

  _tag?: PseudoProp;
  tag?: Renderable;

  _media?: PseudoProp;
  media?: Renderable;

  _body?: PseudoProp;
  body?: Renderable;

  _footer?: PseudoProp;
  footer?: Renderable;
}

type CardVariant = "normal" | "elevated" | "outline";
type CardSize = never;
```

Card has 4 default variants and sizes.

## Variants

<ComponentDisplay>
  {[
    [
      <Card v="elevated">
        <x.p fontSize="4rem" lineHeight="4rem">
          📦
        </x.p>
        <x.h3 mt="1.5rem">
          Ready out of the box
        </x.h3>
        <x.p mt="1rem">Just install and use, Anolis is easy to setup and comes with variety of ready-to-use components.</x.p>
      </Card>,
      `
<Card v="elevated">
  <x.p fontSize="4rem" lineHeight="4rem">
    📦
  </x.p>
  <x.h3 mt="1.5rem">
    Ready out of the box
  </x.h3>
  <x.p mt="1rem">Just install and use, Anolis is easy to setup and comes with variety of ready-to-use components.</x.p>
</Card>
      `
    ]
  ]}
</ComponentDisplay>



# Sandbox

<ComponentSandbox />
<ComponentSandbox />



